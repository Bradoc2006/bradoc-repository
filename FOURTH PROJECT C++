#include <iostream>
#include <cmath>

using namespace std;

int main() {
    double raio, area;

    cout << "Digite o raio do circulo: ";
    cin >> raio;

    area = M_PI * pow(raio, 2);

    cout << "A area do circulo e: " << area << endl;

    return 0;
}
